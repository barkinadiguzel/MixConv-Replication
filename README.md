# MixConv-Replication
MixConv-Replication is a minimal implementation of Mixed Depthwise Convolutions, focusing on translating the paper’s architectural idea into clean, readable code. There is no training or evaluation; the goal is to clearly demonstrate how multiple kernel sizes are mixed across channels in practice.
